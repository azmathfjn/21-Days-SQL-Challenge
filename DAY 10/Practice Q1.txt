SELECT 
    patient_id,
    name,
    satisfaction,
    CASE
        WHEN satisfaction >= 80 THEN 'High'
        WHEN satisfaction >= 60 THEN 'Medium'
        ELSE 'Low'
    END AS satisfaction_level
FROM patients;