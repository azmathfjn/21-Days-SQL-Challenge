SELECT 
    s.staff_id,
    s.staff_name,
    s.service,
    (
        SELECT COUNT(*)
        FROM patients p
        WHERE p.service = s.service
    ) AS total_patients_in_service
FROM staff s;
