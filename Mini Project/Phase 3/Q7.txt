SELECT 
    p1.pizza_type_id,
    p1.size AS size1,
    p2.size AS size2,
    p1.price - p2.price AS price_difference
FROM pizzas p1
JOIN pizzas p2 
    ON p1.pizza_type_id = p2.pizza_type_id
   AND p1.size <> p2.size;